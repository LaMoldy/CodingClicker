import { createStore } from 'vuex';

/** Where global variables for the entire application will be stored */
export const store = createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
